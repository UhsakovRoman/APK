<?xml version="1.0" encoding="utf-8" standalone="no"?><manifest xmlns:android="http://schemas.android.com/apk/res/android" package="home.solo.launcher.free.theme.helloKittyThemeCracked">
    <application android:icon="@drawable/icon" android:label="@string/app_name" android:theme="@style/ThemeHolo">
        <activity android:label="@string/app_name" android:name="home.solo.launcher.free.theme.helloKittyThemeCracked.ThemeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <action android:name="home.solo.launcher.free.THEMES"/>
                <action android:name="home.solo.launcher.free.ACTION_ICON"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <receiver android:name="home.solo.launcher.free.theme.util.ThemeControlReceiver">
            <intent-filter>
                <action android:name="home.solo.launcher.free.action.LAUNCHER_START"/>
            </intent-filter>
            <intent-filter>
                <action android:name="home.solo.launcher.free.action.INACTIVE_APPLY_THEME_FLAG"/>
            </intent-filter>
        </receiver>
    </application>
</manifest>